name: Deploy

# Runs ONLY when the CI workflow succeeds on main. If validation fails,
# nothing is deployed — the merge to main is the trigger, CI is the gate.
on:
  workflow_run:
    workflows: [CI]
    types: [completed]
    branches:
      - main

permissions:
  contents: write
  pages: write
  id-token: write

concurrency:
  group: deploy
  cancel-in-progress: true

jobs:
  build:
    name: Build widget + Storybook
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: chatwidget_components_lit

    steps:
      - name: Checkout validated commit
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.workflow_run.head_sha }}

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: chatwidget_components_lit/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Build widget
        run: npm run build

      - name: Build Storybook
        run: npm run build-storybook

      - name: Add test page
        run: cp test.html storybook-static/test.html

      - name: Add widget bundle to deployment
        run: |
          mkdir -p storybook-static/dist
          cp -r dist/* storybook-static/dist/

      - name: Add public client configurations
        run: |
          mkdir -p storybook-static/public/clients
          cp -r public/clients/* storybook-static/public/clients/

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: chatwidget_components_lit/storybook-static

  deploy:
    name: Deploy to GitHub Pages
    needs: build
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest

    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
